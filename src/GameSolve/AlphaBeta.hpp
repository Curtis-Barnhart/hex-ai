#include "../GameState/GameState.hpp"

namespace GameSolve {

template<class A>
class AlphaBetaSolver {
public:

private:
    GameState::State<A> initial;
    bool is_goal_state(GameState::State<A> state);
    double 
};

}

